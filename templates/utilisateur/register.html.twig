{% extends 'layout.html.twig' %}

{% block title %}Inscription{% endblock %}
{% block nav %} {% endblock %}

{% block main %}
    <h1>Inscription</h1>
    {{ form_start(registrationForm) }}
    {{ form_widget(registrationForm) }}
    <button type="submit" class="btn-lg  btn-dark "> Inscription</button>
    {{ form_end(registrationForm) }}
{% endblock %}